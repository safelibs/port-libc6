1. The phase-05a landing instructions still misclassify `safe/scripts/stage-original-build.sh` as part of the preexisting prepared frontier even though it does not exist in the current tree. The plan says the landing phase should "land" that helper (`.plan/plan.md:27,95,208,236-238`), but the same phase's `Preexisting Inputs` section also tells the implementer to treat `safe/scripts/**`, especially `safe/scripts/stage-original-build.sh`, as an already-present input artifact (`.plan/plan.md:192`). In the actual workspace, `safe/scripts/` only contains `build-debs.sh` and `install-safe-repo.sh`; there is no `stage-original-build.sh`. That contradiction matters because the landing phase is explicitly supposed to preserve the prepared tree rather than invent missing artifacts. The plan needs one concrete stance: either move `stage-original-build.sh` entirely into `New Outputs`/implementation work that phase 05a creates from the existing `xtask`/`ingest_baseline` logic, or add a different existing source artifact that the landing phase is supposed to commit instead.

2. The zero-entry test roots are still underspecified in a way that forces the generated phase prompts to invent repository mechanics later. The plan repeatedly requires `safe/tests/nscd`, `safe/tests/po`, and `safe/tests/manual` to become "checked-in zero-entry destination roots" and requires `check-owned-tests` to verify those owned roots exist (`.plan/plan.md:91,437,619,644,794,960,1325`), but it never defines how an empty directory is supposed to be committed or checked. Git does not track empty directories, and the current tree contains no documented sentinel convention such as `.gitkeep`, `.keep`, or per-root README placeholders. As written, a later implementer or workflow writer must invent how those roots become reviewable artifacts, which violates the requirement that the plan be specific enough to generate phase prompts and workflow structure without filling in missing details. The plan should mandate one concrete committed sentinel-file convention for zero-entry roots and update the corresponding checker expectations to validate those exact tracked placeholder paths.
