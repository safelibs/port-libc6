[package]
name = "network-identity"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = [
    'cfg(safelibs_dso_build)',
    'cfg(safelibs_dso, values("libanl", "libresolv"))',
] }
