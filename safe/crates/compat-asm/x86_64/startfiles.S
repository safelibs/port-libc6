/* Final amd64 libc6-dev startfile provenance marker.
 *
 * The package build stages the Debian-compatible crt object payloads under
 * safe/work/libc-family-build/amd64 and records them as compat_asm. This
 * checked-in source is the review anchor for that unavoidable amd64 assembly
 * compatibility surface.
 */

.section .note.GNU-stack,"",@progbits
